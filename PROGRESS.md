# 坦克大战 H5 游戏 - 项目进度

## 文件结构
- `index.html` - 页面结构、HUD、覆盖层（开始/暂停/结束）、移动端触屏按钮，引用外部 css/js
- `css/style.css` - 全部样式
- `script/main.js` - 全部游戏逻辑（canvas 900x600，30x20 网格，CELL=30）

## 已完成功能
- **玩家**：WASD/方向键移动，空格/J 射击，K 放雷，3 生命（❤️ 显示），P 暂停，R 重开
- **敌人**：从左上 `(1,1)`、右上 `(28,1)` 交替出生，最多 7 个，随时间加速/加难度，自带血条（2 HP），AI 偏向追踪玩家
- **道具**（随机定时刷新 + 敌人 28% 掉落）：
  - 🚁 无人机（最多 2 个，自动追踪射击，HP 3）
  - ✨ 散弹（9s 三发）、⚡ 射速（9s）、💨 移速（9s）、🛡️ 护盾（9s，吸收一次伤害）
  - 💣 地雷（+3 个，K 放置）、❤️ 生命恢复
- **墙体（每局随机生成）**：
  - 🟫 砖墙 WALL：子弹反弹一次（反弹后有白圈标记），不可摧毁
  - 🪨 碎石墙 CRACK：HP 2~3，子弹/地雷可击碎，裂纹随耐久变化，顶部白点显示耐久
  - **分区生成**：每局随机左右分或上下分（分割线 13~16），左侧/顶部全为砖墙、右侧/底部全为碎石墙，两区互不穿插
- **传送门 GATE**：两组双向（左上↔右上、正上↔正下），坦克和子弹穿过即传送，坦克传送冷却 1s
- **地图连通性保证**：随机生成后 BFS 校验玩家出生点可达两个敌人刷新点（门可通行），失败则重试（最多 40 次）
- **其他**：得分/最高分（localStorage `tank-hi`）、粒子爆炸、漂浮文字、WebAudio 音效、移动端触屏按键、游戏结束统计

## 关键常量/逻辑
- 地图值：`EMPTY=0 WALL=1 GATE=2 BORDER=3 CRACK=4`
- 出生点：`PLAYER_SPAWN={c:5,r:15}`，`ENEMY_SPAWNS=[{c:1,r:1},{c:28,r:1}]`
- 主要函数：`genMap`（随机墙+连通性）、`placeRandomWalls`（分区生成）、`damageCrack`（碎石墙受击）、`updateBullets`（反弹/穿门/击碎逻辑）、`blocked`（含坦克碰撞）、`tryTeleport`
- 修复过：EMPTY=0 被当假值导致墙体生成失败的 bug（`map[r][c] === undefined` 判断）
- 修复过：`updateBullets` 中地图索引写反（`map[cc.c][cc.r]` → `map[cc.r][cc.c]`），导致列 ≥20 区域子弹被误判撞墙提前消失
- 修复过：子弹 4s 寿命导致中途消失 → 改为仅在出图（越界）或二次碰撞障碍墙时消失，子弹上限 70 → 200 避免发不出子弹
- 修复过：敌人撞墙后仅重置计时器易卡角落 → 新增 `pickEnemyDir`/`dirFree`，撞墙时立即切换为可通行方向
- 修复过：敌人易卡左右上角 → 新增 `freeDist` 优先挑选前方最宽敞方向 + `stuckT` 卡死检测强制换向 + 出生点选拥挤度低的一侧
- 调整：左右传送门位置从顶部（行 4）改为垂直居中（行 9~10），对应保护格同步移动

## 已验证
- JS 语法 `node --check` 通过
- 随机地图算法独立测试 500 局：连通 100%，交叉混排格数 0，平均约 72 砖墙 + 35 碎石墙
- 浏览器未实机测试过移动端触屏布局

## 可能待办/可扩展方向
- 实机浏览器测试（桌面 + 移动端触屏）
- 敌人种类/波次系统、关卡升级
- 音效可开关、难度选择
- 重新开始已能在游戏进行中通过 R/按钮触发（随机新地图）
